spring:
  data:
    mongodb:
      host: mongo
      port: 27017
      username: ${MONGO_INITDB_ROOT_USERNAME}
      password: ${MONGO_INITDB_ROOT_PASSWORD}
      database: users
      authentication-database: admin

server:
  port: 8080
